# OpenRouter BYOK only for reflection generation

Daily Reflection calls OpenRouter using only the user’s API key (plus optional model slug override). We rejected app-hosted inference for MVP to keep journal text off our servers, avoid provider cost on our side, and match a privacy-first Obsidian workflow. Setup UX includes instructions to create an OpenRouter key.
